class weechat::params {
  # If true will install unsigned up-to-date packages from weechat.org
  # This _may_ break, eat your babies or implode the universe, In that
  # order.
  $install_upstream_packages = false
  # If true, will install development packages of client
  $install_devel_packages    = false
}
